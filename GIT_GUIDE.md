# 📚 Пошаговая инструкция по работе с Git репозиторием

## Шаг 1: Инициализация Git репозитория

Если репозиторий еще не создан, выполните:

```bash
git init
```

Эта команда создаст скрытую папку `.git` в вашем проекте.

---

## Шаг 2: Проверка статуса файлов

Посмотрите, какие файлы изменены или добавлены:

```bash
git status
```

Вы увидите список файлов, которые нужно добавить в репозиторий.

---

## Шаг 3: Добавление файлов в staging area

Добавьте все измененные и новые файлы:

```bash
git add .
```

Или добавьте конкретные файлы:
```bash
git add DEPLOY.md
git add render.yaml
git add server/index.ts
git add server/db.ts
git add package.json
git add .gitignore
git add README.md
```

---

## Шаг 4: Создание коммита

Закоммитьте изменения с описательным сообщением:

```bash
git commit -m "Подготовка к деплою на Render"
```

Или более подробное сообщение:
```bash
git commit -m "Подготовка к деплою на Render

- Исправлены CORS настройки для продакшена
- Настроен порт из переменной окружения PORT
- Добавлена поддержка SSL для PostgreSQL на Render
- Создан render.yaml для автоматического деплоя
- Добавлена инструкция DEPLOY.md
- Обновлен .gitignore для .env файлов"
```

---

## Шаг 5: Подключение к удаленному репозиторию (если еще не подключен)

Если у вас еще нет удаленного репозитория на GitHub/GitLab/Bitbucket:

### 5.1. Создайте репозиторий на GitHub:
1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (или **"+"** → **"New repository"**)
3. Введите имя репозитория (например: `pension-fund`)
4. Выберите **Public** или **Private**
5. **НЕ** ставьте галочки на "Initialize with README" (у вас уже есть файлы)
6. Нажмите **"Create repository"**

### 5.2. Подключите локальный репозиторий к удаленному:

GitHub покажет вам команды после создания репозитория. Обычно это:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/pension-fund.git
```

Или если используете SSH:
```bash
git remote add origin git@github.com:ВАШ_USERNAME/pension-fund.git
```

**Замените `ВАШ_USERNAME` на ваш логин GitHub!**

Проверьте, что репозиторий подключен:
```bash
git remote -v
```

---

## Шаг 6: Отправка изменений на GitHub

Отправьте ваши коммиты в удаленный репозиторий:

```bash
git push -u origin main
```

Или если ваша основная ветка называется `master`:
```bash
git push -u origin master
```

**Примечание**: При первом push может потребоваться авторизация:
- Если используете HTTPS, введите логин и пароль (или Personal Access Token)
- Если используете SSH, убедитесь, что SSH ключ настроен

---

## Шаг 7: Проверка результата

Откройте ваш репозиторий на GitHub в браузере и убедитесь, что все файлы загружены.

---

## 🔄 Обновление репозитория в будущем

Когда вы внесете изменения в проект, повторяйте шаги 2-6:

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Закоммитить
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push
```

---

## 📝 Полезные команды Git

### Просмотр истории коммитов:
```bash
git log
```

### Просмотр изменений в файлах:
```bash
git diff
```

### Отмена изменений в файле (до добавления в staging):
```bash
git checkout -- имя_файла
```

### Отмена добавления файла из staging:
```bash
git reset HEAD имя_файла
```

### Просмотр информации о удаленном репозитории:
```bash
git remote show origin
```

---

## ⚠️ Важные замечания

1. **НЕ коммитьте** файл `.env` - он уже добавлен в `.gitignore`
2. **НЕ коммитьте** `node_modules` - они тоже в `.gitignore`
3. Всегда проверяйте `git status` перед коммитом
4. Используйте понятные сообщения коммитов

---

## 🆘 Если что-то пошло не так

### Отменить последний коммит (но сохранить изменения):
```bash
git reset --soft HEAD~1
```

### Отменить последний коммит (и удалить изменения):
```bash
git reset --hard HEAD~1
```

### Обновить локальный репозиторий с удаленного:
```bash
git pull origin main
```

---

Удачи! 🚀

