# Инструкция по деплою на Render

Это руководство поможет вам задеплоить сайт ПенсионныйФонд.РФ на платформу Render.

## 📋 Предварительные требования

1. Аккаунт на [Render.com](https://render.com)
2. GitHub репозиторий с вашим кодом (или GitLab/Bitbucket)

## 🚀 Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены и запушены в ваш Git репозиторий:
   ```bash
   git add .
   git commit -m "Подготовка к деплою на Render"
   git push
   ```

## 🗄️ Шаг 2: Создание базы данных PostgreSQL на Render

1. Войдите в [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните форму:
   - **Name**: `pension-fund-db` (или любое другое имя)
   - **Database**: `pension_fund`
   - **User**: `pension_user` (или оставьте по умолчанию)
   - **Region**: Выберите ближайший регион
   - **PostgreSQL Version**: Выберите последнюю версию
   - **Plan**: `Free` (для начала)
4. Нажмите **"Create Database"**
5. **ВАЖНО**: Дождитесь полного создания базы данных (это может занять несколько минут)
6. После создания, скопируйте **Internal Database URL** - он понадобится позже

## 🌐 Шаг 3: Создание Web Service на Render

1. В Render Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш Git репозиторий:
   - Выберите ваш провайдер (GitHub/GitLab/Bitbucket)
   - Выберите репозиторий с проектом
   - Нажмите **"Connect"**

3. Заполните настройки сервиса:
   - **Name**: `pension-fund` (или любое другое имя)
   - **Region**: Выберите тот же регион, что и для базы данных
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: Оставьте пустым (корень проекта)
   - **Runtime**: `Node`
   - **Build Command**: `NPM_CONFIG_PRODUCTION=false npm install && npm run build`
   - **Start Command**: `npm run db:push && npm start`
   - **Plan**: `Free` (для начала)

4. Нажмите **"Advanced"** и добавьте переменные окружения:

   ### Обязательные переменные:
   ```
   NODE_ENV=production
   DB_SSL=true
   JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
   ```

   ### Для базы данных:
   - Нажмите **"Add Database"** и выберите созданную ранее базу данных
   - Render автоматически добавит переменную `DATABASE_URL`

   ### Опциональные переменные:
   ```
   ADMIN_DEFAULT_EMAIL=admin@pension.ru
   ADMIN_DEFAULT_PASSWORD=admin123
   ALLOWED_ORIGIN=https://ваш-домен.onrender.com
   ```

5. Нажмите **"Create Web Service"**

## ⏳ Шаг 4: Ожидание деплоя

1. Render начнет процесс деплоя автоматически
2. Вы увидите логи сборки в реальном времени
3. Первый деплой может занять 5-10 минут
4. После успешной сборки, ваш сайт будет доступен по адресу:
   `https://pension-fund.onrender.com` (или ваш кастомный домен)

## 🔧 Шаг 5: Применение миграций базы данных

Миграции базы данных уже включены в **Start Command** (`npm run db:push && npm start`), поэтому они будут применяться автоматически при каждом запуске сервиса.

**Примечание**: `drizzle-kit push` безопасно выполнять многократно - он не создаст таблицы заново, если они уже существуют.

**Альтернативный способ** (если хотите применять миграции вручную):
1. В Render Dashboard откройте ваш Web Service
2. Перейдите в раздел **"Shell"** (требуется платный план)
3. Выполните команду: `npm run db:push`

## 🔧 Шаг 6: Настройка после деплоя

### Проверка работы:
1. Откройте ваш сайт в браузере
2. Проверьте, что главная страница загружается
3. Попробуйте зарегистрироваться
4. Войдите с учетными данными администратора (если настроили)

### Создание первого администратора:
Если вы не настроили переменные `ADMIN_DEFAULT_EMAIL` и `ADMIN_DEFAULT_PASSWORD`, 
первый администратор будет создан автоматически при первом запуске с дефолтными данными.

## 🔄 Обновление сайта

После каждого push в основную ветку репозитория, Render автоматически:
1. Обнаружит изменения
2. Запустит новую сборку
3. Задеплоит обновленную версию

Вы можете отключить автоматический деплой в настройках сервиса и запускать деплой вручную.

## 🐛 Решение проблем

### Проблема: База данных не подключается
**Решение**: 
- Убедитесь, что база данных полностью создана
- Проверьте, что переменная `DATABASE_URL` установлена
- Убедитесь, что `DB_SSL=true` установлен

### Проблема: Сборка падает или файл dist/index.js не найден
**Решение**:
1. **Проверьте логи сборки (Build Logs)** в Render Dashboard:
   - Откройте Web Service → Logs
   - Найдите раздел "Build" (не "Runtime")
   - Проверьте, что команды `vite build` и `esbuild` выполнились успешно
   - Ищите ошибки компиляции или отсутствующие зависимости

2. **Если сборка проходит успешно, но файл не найден**:
   - Убедитесь, что Build Command правильный: `NPM_CONFIG_PRODUCTION=false npm install && npm run build`
   - Проверьте, что в логах сборки есть сообщение об успешном создании `dist/index.js`
   - Убедитесь, что Start Command правильный: `npm run db:push && npm start`

3. **Если esbuild падает с ошибкой**:
   - Проверьте, что все зависимости установлены (особенно `esbuild`)
   - Убедитесь, что нет ошибок TypeScript компиляции
   - Проверьте логи на наличие ошибок импорта модулей

### Проблема: Сайт не запускается
**Решение**:
- Проверьте логи приложения (Runtime Logs) в Render Dashboard
- Убедитесь, что порт берется из переменной `PORT` (это уже настроено)
- Проверьте, что команда `npm start` корректна
- Убедитесь, что файл `dist/index.js` существует (проверьте логи сборки)

### Проблема: CORS ошибки
**Решение**:
- Установите переменную `ALLOWED_ORIGIN` на ваш домен Render
- Или оставьте её пустой для разрешения всех источников в продакшене

## 📝 Примечания

- **Free план**: Сервисы на бесплатном плане "засыпают" после 15 минут неактивности. Первый запрос после пробуждения может занять 30-60 секунд.
- **База данных**: На бесплатном плане база данных также может "засыпать". Это нормально для тестирования.
- **Лимиты**: Free план имеет ограничения по ресурсам. Для продакшена рекомендуется использовать платные планы.

## 🔐 Безопасность

- **НЕ коммитьте** файл `.env` в Git
- Используйте сильные секретные ключи для `JWT_SECRET`
- Регулярно обновляйте зависимости
- Используйте HTTPS (Render предоставляет его автоматически)

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте документацию Render: https://render.com/docs
3. Убедитесь, что все переменные окружения установлены правильно

---

Удачи с деплоем! 🚀

