# ПенсионныйФонд.РФ - Система управления пенсионным фондом

Полнофункциональный веб-сайт для пенсионного фонда с React фронтендом, Express бэкендом и PostgreSQL базой данных.

## 🚀 Технологии

### Фронтенд
- **React 18** с TypeScript
- **Wouter** для навигации между страницами
- **Swiper** для слайдера на главной странице
- **Shadcn/UI** компоненты с Tailwind CSS
- **TanStack Query** для работы с API

### Бэкенд
- **Node.js + Express** сервер
- **PostgreSQL** база данных с Drizzle ORM
- **JWT** токены для аутентификации
- **bcryptjs** для хеширования паролей

## 📋 Функциональность

### Навигационное меню
- **Главная** - слайдер с тематическими изображениями пожилых людей
- **О нас** - информация о фонде с актуальной статистикой (40.8 млн пенсионеров)
- **Новости** - актуальные новости пенсионной системы через NewsAPI
- **Контакты** - форма обратной связи
- **Клиенты** - админ-панель для управления клиентской базой (только для администраторов)
- **Вход/Регистрация** - система аутентификации

### REST API Маршруты
- `POST /api/auth/register` — регистрация пользователя
- `POST /api/auth/login` — вход в систему
- `GET /api/auth/me` — получение информации о текущем пользователе
- `GET /api/clients` — список всех клиентов (только для админов)
- `GET /api/clients/:id` — информация о конкретном клиенте
- `POST /api/clients` — создание нового клиента
- `PUT /api/clients/:id` — обновление данных клиента
- `DELETE /api/clients/:id` — удаление клиента
- `POST /api/contact` — отправка сообщения через форму обратной связи

### База данных
**Таблица Users:**
- id, email, password, firstName, lastName, isAdmin, createdAt

**Таблица Clients:**
- id, firstName, lastName, email, birthDate, status, registrationDate, createdBy

## 🛠 Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка переменных окружения
Создайте файл `.env` в корне проекта:

```env
# База данных PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/pension_fund
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=pension_fund

# JWT секретный ключ
JWT_SECRET=your-super-secret-jwt-key-here

# Данные первого администратора (необязательно)
ADMIN_DEFAULT_EMAIL=admin@pension.ru
ADMIN_DEFAULT_PASSWORD=admin123
```

### 3. Создание и настройка базы данных
```bash
# Применить схему к базе данных
npm run db:push
```

### 4. Запуск приложения
```bash
# Запуск в режиме разработки
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📊 Статистические данные

В системе используются актуальные данные о пенсионной системе России:
- 40.8 млн пенсионеров в РФ
- 10.3 трлн рублей выплачено пенсий в год
- 33+ лет работы пенсионного фонда

## 📰 Раздел "Новости"

### Функциональность
- 10 актуальных новостей о пенсионной системе России
- Категоризация новостей (Индексация, Социальная поддержка, Льготы и др.)
- Цветные иконки для разных категорий новостей
- Отображение источника новостей (ТАСС, РБК, ПФР, Социальный фонд России)
- Кнопка "Подробнее" для получения дополнительной информации
- Отзывчивая сетка с карточками новостей и тематическими изображениями

## 🖼 Изображения

Все изображения в проекте тематические:
- Слайдер на главной: счастливые пожилые пары, семьи трех поколений
- Страница "О нас": флаг Российской Федерации
- Alt-тексты описывают содержимое изображений на русском языке

## 👥 Роли пользователей

### Обычный пользователь
- Просмотр публичных страниц
- Регистрация и вход в систему
- Отправка сообщений через форму обратной связи

### Администратор
- Все возможности обычного пользователя
- Доступ к странице "Клиенты"
- Управление клиентской базой (создание, редактирование, удаление)
- Поиск и фильтрация клиентов

## 🔧 Структура проекта

```
├── client/src/          # React фронтенд
│   ├── components/      # UI компоненты
│   ├── pages/          # Страницы приложения
│   ├── hooks/          # React хуки
│   └── lib/            # Утилиты и настройки
├── server/             # Express бэкенд
│   ├── routes.ts       # API маршруты
│   ├── storage.ts      # Работа с базой данных
│   └── db.ts          # Подключение к PostgreSQL
├── shared/             # Общие типы и схемы
└── README.md          # Этот файл
```

## 🚀 Деплой

### Деплой на Render

Проект готов для развертывания на Render. Подробная инструкция находится в файле [DEPLOY.md](./DEPLOY.md).

**Быстрый старт:**
1. Создайте PostgreSQL базу данных на Render
2. Создайте Web Service и подключите ваш репозиторий
3. Установите переменные окружения (см. DEPLOY.md)
4. Render автоматически задеплоит ваш сайт

### Деплой на Replit

Проект также готов для развертывания на Replit:
1. Импортируйте проект в Replit
2. PostgreSQL база данных создается автоматически
3. Запустите `npm run dev` для старта приложения

## 📝 Лицензия

Все права защищены © 2023 ПенсионныйФонд.РФ